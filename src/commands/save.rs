use std::path::Path;

pub fn run(
    _config: Option<&Path>,
    _message: Option<&str>,
    _interactive: bool,
) -> anyhow::Result<()> {
    eprintln!("polydot save: not yet implemented");
    Ok(())
}
